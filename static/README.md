# Static folder

This folder contains the necessary data for DOS build:

* CWSPDMI.EXE (Dos extender)
* *.REC (recorded playgame for tutorial and attract demos)
* MANUAL.TXT (Game manual)
* dosbox.conf (dosbox config for running the build (this don't be copied to final release))